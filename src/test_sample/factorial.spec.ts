/*
import { factorial } from "./factorial";

// Si le pones f delante de describe la fijas y es la que se ejecuta
/*fdescribe('Pruebas de testeo', () => {
    // Si le pones f delante de it la fijas y es la que se ejecuta
    it('verdad deberia ser verdad', () => {
        expect(true).toBeTruthy();
    });
});*/
/*fdescribe('Caculo de un factorial', () => {
    it('deberia ser 1 factorial de 0', () => {
       expect(factorial(0)).toEqual(1);
    });
    it('deberia ser 1 factorial de 1', () => {
        expect(factorial(1)).toEqual(1);
    });
    it('deberia ser 120 factorial de 5', () => {
        expect(factorial(5)).toEqual(120);
    });
});
*/
