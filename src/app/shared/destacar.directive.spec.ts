import { DestacarDirective } from './destacar.directive';

describe('DestacarDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacarDirective(null);
    expect(directive).toBeTruthy();
  });
});
