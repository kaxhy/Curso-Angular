import { HacerEditableDirective } from './hacer-editable.directive';

describe('HacerEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new HacerEditableDirective();
    expect(directive).toBeTruthy();
  });
});
